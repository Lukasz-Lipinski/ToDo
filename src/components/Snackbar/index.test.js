import { Snackbar } from "..";

import { screen, render } from "@testing-library/react";
import { Provider } from 'react-redux';

test("should have text message", () => {

});

test("should fade out after 3 seconds", () => { });

test("should change state after 3 seconds", () => { });